import { ChangeDetectionStrategy, Component, inject } from '@angular/core';
import { AsyncPipe, NgFor, NgIf } from '@angular/common';
import { FormControl, ReactiveFormsModule } from '@angular/forms';
import { MatButtonModule } from '@angular/material/button';
import { MatCardModule } from '@angular/material/card';
import { MatChipsModule } from '@angular/material/chips';
import { MatFormFieldModule } from '@angular/material/form-field';
import { MatInputModule } from '@angular/material/input';
import { MatProgressBarModule } from '@angular/material/progress-bar';
import { MatToolbarModule } from '@angular/material/toolbar';
import { debounceTime, defer, distinctUntilChanged, shareReplay, startWith, switchMap, tap } from 'rxjs';
import { CustomerApiService } from './core/customer-api.service';
import { CustomerSummary } from './core/models';

@Component({
  selector: 'c360-root',
  standalone: true,
  imports: [AsyncPipe, NgFor, NgIf, ReactiveFormsModule, MatButtonModule, MatCardModule, MatChipsModule, MatFormFieldModule, MatInputModule, MatProgressBarModule, MatToolbarModule],
  template: `
    <div class="app-shell">
      <aside class="rail">
        <div class="brand">Ledger <span>360</span></div>
        <p class="rail-kicker">RELATIONSHIP<br>OPERATIONS</p>
        <nav><button class="nav-item active"><span class="nav-glyph">⌂</span>Portfolio</button><button class="nav-item"><span class="nav-glyph">↗</span>Service queue</button><button class="nav-item"><span class="nav-glyph">◌</span>Copilot</button></nav>
        <div class="rail-foot"><div class="avatar">RM</div><div><strong>Rohan Mehta</strong><small>Relationship Manager</small></div></div>
      </aside>
      <main class="content">
        <mat-toolbar class="topbar"><div><span class="top-kicker">HYDERABAD CLUSTER / LIVE BOOK</span><strong>Customer workspace</strong></div><button mat-stroked-button class="secure"><span class="status-dot"></span> Role access: RM</button></mat-toolbar>
        <section class="hero"><div><p class="eyebrow">YOUR PORTFOLIO / AUG 2026</p><h1>Good relationships start<br>with the right signal.</h1><p class="hero-copy">A focused view of the customers, risks, and follow-ups that need your attention today.</p></div><div class="hero-orbit"><span>360</span><small>VIEW</small></div></section>
        <section class="metrics"><div class="metric"><span class="metric-label">Customers in book</span><strong>3</strong><span class="metric-note">All relationship-owned</span></div><div class="metric warm"><span class="metric-label">Open requests</span><strong>{{ openRequests$ | async }}</strong><span class="metric-note">Needs follow-through</span></div><div class="metric mint"><span class="metric-label">Portfolio health</span><strong>82<span class="unit">%</span></strong><mat-progress-bar mode="determinate" value="82"></mat-progress-bar></div></section>
        <section class="workspace-head"><div><p class="eyebrow">CUSTOMER DIRECTORY</p><h2>Search the relationship book</h2></div><mat-form-field appearance="outline" class="search-field"><mat-label>Name, ID, email, or phone</mat-label><span matPrefix>⌕&nbsp;</span><input matInput [formControl]="query"></mat-form-field></section>
        <section class="directory"><div class="directory-list"><div class="list-meta"><span>{{ (customers$ | async)?.length || 0 }} visible customers</span><span>Sorted by attention</span></div><button class="customer-row" *ngFor="let customer of customers$ | async" [class.selected]="selectedCustomer?.id === customer.id" (click)="selectCustomer(customer)"><div class="customer-initials">{{ customer.name.slice(0, 2).toUpperCase() }}</div><div class="customer-main"><strong>{{ customer.name }}</strong><span>{{ customer.id }} · {{ customer.email }}</span></div><mat-chip-set><mat-chip>{{ customer.segment }}</mat-chip><mat-chip [class.risk-chip]="customer.riskFlag === 'Medium'">{{ customer.riskFlag }} risk</mat-chip></mat-chip-set><div class="request-count"><strong>{{ customer.openServiceRequests }}</strong><span>open<br>requests</span></div></button></div><aside class="detail-panel"><ng-container *ngIf="selectedCustomer as customer"><p class="eyebrow">SELECTED RELATIONSHIP</p><div class="detail-avatar">{{ customer.name.slice(0, 2).toUpperCase() }}</div><h2>{{ customer.name }}</h2><p class="detail-id">{{ customer.id }} · {{ customer.segment }} segment</p><div class="detail-divider"></div><dl><dt>Contact</dt><dd>{{ customer.email }}<br>{{ customer.phone }}</dd><dt>Risk posture</dt><dd><span class="risk-marker" [class.medium]="customer.riskFlag === 'Medium'"></span>{{ customer.riskFlag }} risk</dd><dt>Next focus</dt><dd>{{ customer.openServiceRequests ? 'Review open service request' : 'Plan a relationship check-in' }}</dd></dl><button mat-flat-button class="open-profile">Open 360° profile <span>→</span></button></ng-container><div class="empty-detail" *ngIf="!selectedCustomer">Select a customer to see their relationship signal.</div></aside></section>
      </main>
    </div>
  `,
  styles: [`:host { display:block; min-height:100vh; background:#f3f4ef; color:#172231; } * { box-sizing:border-box; } .app-shell { display:grid; grid-template-columns:230px minmax(0,1fr); min-height:100vh; } .rail { background:#122b35; color:#e4eee8; padding:28px 20px; display:flex; flex-direction:column; } .brand { font:600 27px Newsreader, Georgia, serif; letter-spacing:-.02em; } .brand span { color:#e9a36d; font:700 11px 'DM Sans',sans-serif; letter-spacing:.13em; margin-left:5px; } .rail-kicker,.top-kicker,.eyebrow { color:#d68b5c; letter-spacing:.14em; font:700 10px 'DM Sans',sans-serif; line-height:1.6; } .rail-kicker { color:#92b5ae; margin:48px 0 16px; } nav { display:grid; gap:6px; } .nav-item { background:transparent; border:0; color:#b9d0ca; padding:11px 10px; text-align:left; border-radius:6px; font:500 13px 'DM Sans',sans-serif; cursor:pointer; } .nav-item.active,.nav-item:hover { background:#214752; color:#fff; } .nav-glyph { display:inline-block; width:26px; color:#e9a36d; font-size:16px; } .rail-foot { margin-top:auto; border-top:1px solid #31525a; padding-top:18px; display:flex; gap:10px; align-items:center; font-size:12px; } .rail-foot small { display:block; color:#92b5ae; margin-top:3px; font-size:10px; } .avatar,.detail-avatar,.customer-initials { background:#e9a36d; color:#122b35; width:34px; height:34px; border-radius:50%; display:grid; place-items:center; font-size:11px; font-weight:700; } .content { min-width:0; padding:0 clamp(20px,4vw,58px) 60px; } .topbar { background:transparent; padding:25px 0; height:auto; color:#172231; display:flex; justify-content:space-between; } .topbar strong { display:block; font:600 17px Newsreader,Georgia,serif; } .top-kicker { color:#6c7a82; font-size:9px; } .secure { font-size:11px; border-color:#cbd8d0; color:#28756d; } .status-dot { display:inline-block; width:7px; height:7px; border-radius:50%; background:#28756d; margin-right:6px; } .hero { display:flex; justify-content:space-between; align-items:center; background:#e1ece6; border:1px solid #c9ddd4; border-radius:9px; padding:34px 42px; overflow:hidden; } h1 { font:600 clamp(37px,4.3vw,63px)/.94 Newsreader,Georgia,serif; letter-spacing:-.025em; margin:8px 0 16px; } .hero-copy { color:#53666b; max-width:420px; font-size:14px; line-height:1.5; } .hero-orbit { width:150px; height:150px; border:1px solid #9ac0b3; border-radius:50%; display:grid; place-items:center; color:#28756d; position:relative; transform:rotate(-12deg); } .hero-orbit:before { content:''; position:absolute; inset:13px; border:1px dashed #9ac0b3; border-radius:50%; } .hero-orbit span { font:600 50px Newsreader,Georgia,serif; } .hero-orbit small { position:absolute; bottom:28px; font-size:9px; letter-spacing:.16em; } .metrics { display:grid; grid-template-columns:repeat(3,1fr); gap:12px; margin:14px 0 42px; } .metric { background:#fff; border:1px solid #d7e0d9; border-radius:7px; padding:17px 19px; min-height:112px; } .metric.warm { background:#fbf0e7; border-color:#efd4bd; } .metric.mint { background:#eaf3ed; border-color:#cfdfd2; } .metric-label,.metric-note { display:block; color:#65737b; font-size:11px; } .metric strong { display:block; color:#172231; font:600 34px Newsreader,Georgia,serif; margin:8px 0 3px; } .metric .unit { font-size:18px; margin-left:2px; } mat-progress-bar { max-width:140px; height:6px; } .workspace-head { display:flex; justify-content:space-between; align-items:end; gap:20px; margin-bottom:12px; } h2 { font:600 25px Newsreader,Georgia,serif; margin:3px 0 0; } .search-field { width:min(380px,100%); } .directory { display:grid; grid-template-columns:minmax(0,1.55fr) minmax(260px,.75fr); gap:14px; } .directory-list,.detail-panel { background:#fff; border:1px solid #d7e0d9; border-radius:8px; padding:16px; } .list-meta { display:flex; justify-content:space-between; color:#819098; font-size:11px; padding:0 7px 10px; } .customer-row { width:100%; display:flex; align-items:center; gap:12px; border:1px solid transparent; background:#fff; border-radius:7px; padding:13px 8px; text-align:left; cursor:pointer; } .customer-row:hover,.customer-row.selected { background:#f2f7f3; border-color:#c6ddd1; } .customer-initials { flex:0 0 36px; width:36px; height:36px; background:#dcebe2; color:#28756d; } .customer-main { min-width:150px; flex:1; } .customer-main strong,.customer-main span { display:block; } .customer-main strong { font-size:13px; } .customer-main span { color:#7a878d; font-size:10px; margin-top:3px; overflow:hidden; text-overflow:ellipsis; } mat-chip { --mdc-chip-label-text-size:10px; } .risk-chip { background:#f9e5d9 !important; color:#a64f2d !important; } .request-count { display:flex; gap:6px; align-items:center; color:#7a878d; font-size:9px; text-transform:uppercase; line-height:1.1; } .request-count strong { color:#c56f3e; font:600 22px Newsreader,Georgia,serif; } .detail-panel { background:#122b35; color:#edf4ef; min-height:280px; } .detail-panel .eyebrow { color:#e9a36d; } .detail-avatar { width:52px; height:52px; margin:17px 0 10px; font-size:15px; } .detail-panel h2 { color:#fff; font-size:28px; } .detail-id { color:#a9c1bb; font-size:11px; } .detail-divider { height:1px; background:#31525a; margin:18px 0; } dl { display:grid; grid-template-columns:80px 1fr; gap:12px 10px; font-size:11px; } dt { color:#8fb2a9; } dd { margin:0; color:#f1f6f1; line-height:1.5; } .risk-marker { display:inline-block; width:7px; height:7px; background:#72c1a5; border-radius:50%; margin-right:6px; } .risk-marker.medium { background:#e9a36d; } .open-profile { width:100%; margin-top:22px; background:#e9a36d !important; color:#122b35 !important; } .open-profile span { margin-left:auto; } .empty-detail { color:#a9c1bb; font-size:12px; padding:35px 10px; } @media(max-width:850px) { .app-shell { grid-template-columns:1fr; } .rail { display:none; } .content { padding:0 16px 36px; } .hero { padding:26px 22px; } .hero-orbit { width:105px; height:105px; } .hero-orbit span { font-size:38px; } .directory { grid-template-columns:1fr; } } @media(max-width:620px) { .hero-orbit { display:none; } .metrics { grid-template-columns:1fr; margin-bottom:28px; } .workspace-head { display:block; } .search-field { margin-top:12px; width:100%; } .customer-row { align-items:flex-start; flex-wrap:wrap; } mat-chip-set { order:4; margin-left:48px; } .request-count { margin-left:auto; } }`],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class AppComponent {
  private readonly api = inject(CustomerApiService);
  readonly query = new FormControl('', { nonNullable: true });
  private readonly demoSession$ = defer(() => this.api.loginDemo()).pipe(tap(({ token }) => localStorage.setItem('c360_token', token)), shareReplay(1));
  readonly customers$ = this.demoSession$.pipe(switchMap(() => this.query.valueChanges.pipe(startWith(''), debounceTime(150), distinctUntilChanged(), switchMap((query) => this.api.search(query)))), shareReplay(1));
  readonly openRequests$ = this.customers$.pipe(switchMap((customers) => [customers.reduce((total, customer) => total + customer.openServiceRequests, 0)]));
  selectedCustomer: CustomerSummary | null = null;

  selectCustomer(customer: CustomerSummary): void {
    this.selectedCustomer = customer;
  }
}
