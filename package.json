{
  "name": "customer360-mock-server",
  "version": "0.1.0",
  "description": "Mock API server for the Customer 360 & Relationship Copilot — implements api/openapi.yaml over in-memory mock data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
